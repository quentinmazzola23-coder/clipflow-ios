//
//  BuildInfo.swift
//  ClipFlow
//
//  Écrasé par la CI à chaque build (numéro de run + horodatage Paris).
//  Les valeurs ci-dessous ne servent qu'aux compilations locales.
//

enum BuildInfo {
    static let version = "1.0"
    static let build = "local"
    static let stamp = "compilation locale"
}
